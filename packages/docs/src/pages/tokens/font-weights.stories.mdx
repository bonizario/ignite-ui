import { fontWeights } from '@bonizario-ignite-ui/tokens';
import { Meta, Unstyled } from '@storybook/blocks';

import { TokensGrid } from '../../components/TokensGrid';

<Meta title="Tokens/Font Weights" />

# Font Weights

Font weights are used to determine the thickness of the text, allowing to emphasize and create a visual hierarchy in the design.

<Unstyled>
  <TokensGrid tokens={fontWeights} />
</Unstyled>
